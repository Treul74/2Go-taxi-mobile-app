# Prompt 23 — Update types index audit reports reference
read AGENTS.md first and follow it strictly
Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.

In @src/types/index.ts around lines 6 - 12, Update the explanatory comment near the passenger/driver literal in src/types/index.ts to reference the supplied terminology report under audit_reports/ rather than audit_export/, preserving the existing naming rationale and report filename.
